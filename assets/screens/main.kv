#:import Tabs tabs.Tabs
#:import Info intro.Info

<BaseMDNavigationItem>

    MDNavigationItemIcon:
        icon: root.icon

    MDNavigationItemLabel:
        text: root.text


<Principal>
    orientation: 'vertical'
    id: principal
    MDLabel:
        id: title
        text:
            'atribuir tarefa' if root.ids.screen_manager.current == 'popup' \
            else root.ids.screen_manager.current
        theme_text_color: 'Custom'
        text_color: app.background
        theme_font_size: "Custom"
        font_size: "50sp"
        bold: True
        halign: 'center'
        valign: 'center'
        size_hint: 1., None
        height: '100dp'
        theme_bg_color: "Custom"
        md_bg_color: app.background_two
        elevation: 30
        opacity: .6
    Manager:
        id: screen_manager
        md_bg_color: app.background
        Info:
            name: app.title
        Tabs:
            id: segunda
            name: 'segunda'
        Tabs:
            id: terça
            name: 'terça'
        Tabs:
            id: quarta
            name: 'quarta'
        Tabs:
            id: quinta
            name: 'quinta'
        Tabs:
            id: sexta
            name: 'sexta'
        Tabs:
            id: sabado
            name: 'sabado'
        Tabs:
            id: domingo
            name: 'domingo'

    Nav:
        id: nav
        on_switch_tabs: app.on_switch_tabs(*args)
        indicator_color: app.background_two
        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: app.title

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'segunda'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'terça'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'quarta'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'quinta'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'sexta'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'sabado'

        BaseMDNavigationItem:
            icon: "circle" if root.ids.screen_manager.current == self.text else "circle-outline"
            text: 'domingo'
